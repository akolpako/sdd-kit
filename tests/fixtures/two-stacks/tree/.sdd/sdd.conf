# Two stacks: one command each, and no root command — the two are run one
# after the other, never joined into one shell line.
SDD_BUILD_BACKEND=mvn clean verify
SDD_BUILD_FRONTEND=cd web && npm run build && npm test
