GATE=stop
FEATURE=Cart
FOLDER=Cart
STAGE=raw
REVIEW=missing
REVIEW_PATH=_docs/Cart/review.md
REVIEW_DATE=
REVIEW_ROOT=
REVIEW_VERDICT=absent
ROOT=
BUILD_ROOT=
BUILD_BACKEND=
BUILD_FRONTEND=

STOP:
_docs/Cart/new-requirements.md is missing — a fix is written against the spec, and it is not on disk. /sdd-specify Cart writes it.
_docs/Cart/design.md is missing — a fix is written against the spec, and it is not on disk. /sdd-specify Cart writes it.
_docs/Cart/tasks.md is missing — a fix is written against the spec, and it is not on disk. /sdd-specify Cart writes it.
_docs/Cart/review.md is missing — there is no report to act on. /sdd-code-review Cart writes one.

ASK:
No build command for this project: .sdd/sdd.conf holds no SDD_BUILD_ROOT, and no pom.xml, build.gradle or package.json was found to propose one from. Write the command that verifies this project into .sdd/sdd.conf as SDD_BUILD_ROOT=<command> — one line, run from the project root. Nothing is built until it is there.

WARN:
Not a git repository — whether the report still describes the current code cannot be checked. A finding may be about a line that has since moved.

ANOMALIES:

NOTES:
feature-state: No _docs/requirements.md — no feature can be told apart from actualized, and every finished one stays at 'implemented'.
### exit=0
