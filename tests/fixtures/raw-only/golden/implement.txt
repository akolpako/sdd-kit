GATE=stop
FEATURE=Cart
FOLDER=Cart
STAGE=raw
TASKS_DONE=0
TASKS_TOTAL=0
TASKS_BLOCKED=0
ROLE_ORDER=
BUILD_ROOT=
BUILD_BACKEND=
BUILD_FRONTEND=
BASELINE=
BUILD_FILES=0

STOP:
_docs/Cart/new-requirements.md is missing. /sdd-specify Cart writes it.
_docs/Cart/design.md is missing. /sdd-specify Cart writes it.
_docs/Cart/tasks.md is missing. /sdd-specify Cart writes it.

ASK:
Not reviewed: new-requirements.md (no status), design.md (no status), tasks.md (no status). /sdd-spec-review Cart is what promotes a spec to ready. Implementing anyway needs the human to say so in as many words, and the final report then says the work was built from an unreviewed spec.
No build command for this project: .sdd/sdd.conf holds no SDD_BUILD_ROOT, and no pom.xml, build.gradle or package.json was found to propose one from. Write the command that verifies this project into .sdd/sdd.conf as SDD_BUILD_ROOT=<command> — one line, run from the project root. Nothing is built until it is there.

WARN:

ANOMALIES:

NOTES:
feature-state: No _docs/requirements.md — no feature can be told apart from actualized, and every finished one stays at 'implemented'.
### exit=0
