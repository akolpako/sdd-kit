Working directory: <tmp>/no-commit
Every `.sdd/...` and `_docs/...` path below and in the protocols it names is
relative to it.

# Assignment: the project baseline of this codebase

Follow these protocols, in this order:

.sdd/protocols/project-baseline.md
.sdd/protocols/docs-format.md

Files of this run:

_docs/architecture.index.md
_docs/code-style.md
_docs/requirements.md

Already on disk with content in them:

code-style.md,requirements.md,architecture.index.md

Every file above is already on disk: the ones not named just above hold nothing but the
template they were copied from. Write them from the codebase itself, per the protocol —
the structure to keep is the one already in the file, and no template has to be found.

`_docs/architecture.index.md` is the exception: it is generated from the files in
`_docs/architecture/`, and those section files are what you write. Read the index to see
which of them exist, write the ones the codebase settles, delete the ones this project has
nothing to say about — and leave the index itself alone. It is rebuilt once you return.

Return: what you wrote per file, and what the codebase did not settle.
### exit=0
