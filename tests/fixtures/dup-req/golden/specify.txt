GATE=stop
FEATURE=Cart
FOLDER=Cart
STAGE=specified
RAW=present
REQUIREMENTS=present
DESIGN=present
TASKS=present
STATUS_REQUIREMENTS=draft
STATUS_DESIGN=draft
STATUS_TASKS=draft
COMPILATION=present
NEXT_REQ=REQ-004
MODE=draft

REQ_DOMAINS:
-|NEXT=REQ-004|COUNTER=REQ-003|HIGHEST=REQ-002|DECLARED=REQ-003|DECLARED_IN=Cart|COUNT=3

REQ_DUPLICATES:
REQ-002

STOP:
The compilation is already inconsistent — carried by more than one entry: REQ-002. No number is issued until the human repairs it — a REQ-### handed out twice corrupts the compilation quietly.

ASK:
These files already hold a spec: new-requirements.md (draft), design.md (draft), tasks.md (draft). Which of them may this run overwrite? Generation does not start until the human answers, and a file marked ready carries the result of a /sdd-spec-review session that overwriting throws away.

WARN:
_docs/architecture.index.md is not there — the spec is written without the architecture context, and /sdd-init is what creates it.

ANOMALIES:

NOTES:
next-req: 1 number(s) are carried by more than one entry. The compilation is already inconsistent — report it before any number is issued.
### exit=0
